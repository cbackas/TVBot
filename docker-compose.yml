version: '3'
services:
  tvbot:
    image: tvbot:dev
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tvbot
    env_file:
      - .env
    volumes:
      - ./data:/app/data/
